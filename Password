function checkWord() {
  let input = document.getElementById("userInput").value.toLowerCase();
  let output = document.getElementById("instructionBox");

  if (input === "watermelon") {
    output.innerHTML = "Instruction: Find the catapult in the field!";
  } else {
    output.innerHTML = "Try a different word.";
  }
}
